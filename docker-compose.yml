services:
  manager:
    image: cloakbrowser-manager
    container_name: cloakbrowser-manager-container
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ~/.cloakbrowser-manager:/data
    environment:
      - AUTH_TOKEN=${AUTH_TOKEN:-}
